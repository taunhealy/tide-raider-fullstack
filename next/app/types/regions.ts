// app/types/region.ts
import type { Region } from "@/app/types/beaches";

export interface UserSearch {
  id: string;
  region: Region;
}

export interface UserSearch {
  id: string;
  region: Region;
}
